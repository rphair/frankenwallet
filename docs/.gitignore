# Ignore the default location of the built site, and caches and metadata generated by Jekyll
_site/
.sass-cache/
.jekyll-cache/
.jekyll-metadata

# Ignore folders generated by Bundler
.bundle/
vendor/

# (rphair) gitignored for GitHub pages site "built from branch", but since we carry our own
#   versioning requirements for this theme & GitHub Actions I think we have to omit it
#   so the production (public) will can be built the same as our local version:
# Gemfile.lock
